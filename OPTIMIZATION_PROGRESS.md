# 性能与交互优化验收

范围：2026-09-05 项目检查列出的问题及修复引出的回归。更新于 2026-09-07。本轮目标已完成，改动保留在当前工作区，未提交。

## 已完成的优化

- [x] 删除前预览去重后的完整子树，明确运行中和未归档数量；确认后范围扩大则返回 409，保留文件供重新确认。
- [x] 删除排除并发启动，并等待已有提示、分支、复制及异步扩展清理完成；旧扫描不能重新返回或缓存已删除会话。
- [x] 30 天持久回收站，移动失败回滚，恢复拒绝覆盖已有文件，缺失文件不伪报成功；恢复成功后提供直接打开会话的链接。
- [x] SSE 按 32ms 合并增量文本和思考，终止前刷新待发内容，保留重连快照及旧协议兼容；迟到事件不能重新激活已结束的输出。
- [x] 搜索祖先查找改为近线性，共享并发索引、传播取消、设置 64MB 缓存预算；limit+1 确认截断，标题与正文筛选一致，100 条返回结果全部可访问。
- [x] Git 状态查询共享短时缓存，写操作失效；项目切换及请求取消阻止旧结果覆盖当前界面，单个调用方取消不影响其他订阅者。
- [x] 历史消息与流式尾部隔离；聊天、搜索定位和时间线使用有界虚拟窗口。修复父滚动容器 ref 尚未赋值时误用内容高度、导致整段历史挂载的问题。
- [x] 正文、思考、过程分组、工具输出和诊断的展开选择经过卸载后保留；延迟思考及代码块加载、改变宽度、隐藏再显示均有回归覆盖。
- [x] 侧栏使用迭代树和虚拟窗口，保存分支展开状态；键盘可定位离屏任务，拖动保留活动行；隐藏侧栏设为 inert，避免不可见控件获取焦点。
- [x] 设置搜索传递稳定选项 ID，定位、聚焦并高亮具体设置；修复齿轮把点击事件误当分类的问题；浏览器模式排除桌面专属入口。
- [x] 能力设置按安装、配置、连接组织；无项目时解释项目级能力不可用，390px 页面无横向溢出。
- [x] 重命名、删除、恢复和批量部分失败显示真实错误并保留可重试内容；错误提示独占一行，避免挤压重命名输入框；部分删除成功后立即刷新列表。
- [x] 时间线导航完成后关闭临时预览，避免遮挡消息操作；用户固定的预览仍保留。

## 浏览器验收

`scripts/verify-optimization-ui.mjs` 使用独立测试浏览器，在真实应用组件中注入合成响应。修改请求被本地拒绝或明确模拟，不运行模型，也不写入真实会话。

已通过的完整页面检查包括：

- 1,500 行聊天初始挂载及最早消息、搜索第 100 项定位均保持有界；展开内容经回收后恢复，延迟思考只读取一次。
- 从旧历史发送新消息可定位新用户行；32ms 模拟增量持续显示，历史文本无重复改写；结束后保留最终回答并忽略迟到事件。
- 500 个侧栏根任务窗口化；Home/End、分支键盘展开持久化、长按拖动排序及隐藏侧栏焦点隔离通过。
- 字体、自动滚动、完成通知、命令面板的精确设置定位通过；能力入口和无项目说明、390px 布局通过。
- 重命名失败不丢输入且输入框不缩窄；删除失败保留原任务；恢复失败可重试，成功后有打开入口。
- 批量删除确认包含 3 个受影响会话、未归档与运行数量；模拟第一项成功、第二项失败后，仅失败项留在列表并显示操作错误。补验 3 项（含无未捕获浏览器错误）通过。

前一完整页面版本 33 项通过；扩展批量检查后的运行前 32 项通过，最后两项因测试定位中止，随后收紧为设置内容区域的就绪条件并单独复验通过。时间戳报告保存在 `.verification/optimization/report-*.json`，后续失败报告不会覆盖已有验收记录。

`scripts/verify-virtual-list-browser.mjs` 最新 8 项全部通过：真实 VirtualList/VirtualRowState 的 10,000 行窗口、展开恢复、390px、隐藏重显、离屏焦点保留与释放，以及无未捕获错误。旧初始化条件只在生成的对照副本中恢复，重现 10,000 行全挂载并按预期失败；项目源码没有回退。报告：`.verification/virtual-list/report.json`。

浏览器帧间隔样本曾记录 p95 约 16.7ms，属于开发模式下的合成流式场景，不代表所有设备或生产环境帧率。开发服务有间歇性响应超时，最终另开浏览器尝试也未能打开；该失败不记为页面通过，已有页面及独立组件验收结果分别保留。未停止其他任务持有的开发进程。

## 自动检查

最终整套回归：1,398 项，1,393 通过、5 跳过、0 失败，约 347 秒。日志：`$env:TEMP/piora-final-green-suite.log`。网页与桌面 TypeScript、性能预算检查通过。全量 ESLint 无错误、无警告，`git diff --check` 通过。日志分别为 `$env:TEMP/piora-final-clean-{lint,types}.log`、`piora-final-desktop-types.log` 和 `piora-final-performance.log`。

性能函数测试：6,000 条同轮助手消息冷索引约 2,655ms → 21ms；80 个均命中文件、limit=1 时读取 80 → 2 个。最新预算复验：10,000 条聊天渲染计划约 22.44ms，窗口范围约 0.03ms，500 项侧栏索引约 0.95ms，均低于预算。均为同机合成测量，不是页面响应时间。

没有运行 `next build`。未提交、推送或部署；工作区其他任务的改动保留。
