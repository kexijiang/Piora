# Clipboard v2 implementation and acceptance ledger

Goal: implement the approved official-uTools-clipboard plan in full. This file tracks implementation, not a reduced definition of completion.

Product and interaction specification: [随身仓剪贴板完整修改方案](./clipboard-v2-design.md). The benchmark is the **uTools team’s official Clipboard plugin**, with first-party behavior separated from Piora’s recommended design decisions. The specification is a design deliverable, not evidence that the unchecked implementation items below have shipped.

## Latest acceptance evidence (2026-09-10)

### Delivery complete under the user's practical acceptance standard

- Final isolated desktop compile, electron-builder packaging and packaged runtime checks all exited 0. Authoritative final state: `X:/release-workspaces/clipboard-2D11wt/verification-final-state.json`. All 11 clipboard modules match the compiled snapshot, including the final database change; native FFI loads, the SQLite worker runs from ASAR with isolated dependencies, and authenticated clipboard pages return 200.
- Ready-to-run Windows directory package: `X:/release-workspaces/clipboard-2D11wt/desktop/release/win-unpacked/Piora.exe`. Keep its surrounding directory intact. No deployment, installation over the user's current app, or git commit was performed.
- Product implementation and the user's revised acceptance are complete. Existing packaged UI/restart, native paste/drag/IME, database and recovery evidence is retained; no new performance campaign or exhaustive matrix was run. [User delivery notes](./clipboard-v2-delivery.md) summarize the result. Chronological pending statements below describe earlier snapshots, not current blockers.

### User-directed acceptance adjustment and final source-read change

- The user accepts approximately 200 ms at 100k records and explicitly stopped further optimization. They subsequently relaxed exhaustive verification to practical everyday usability. The updated specification records this precedence; no further tail-latency tuning or exhaustive hardware/application matrix is required for delivery. This does not recast earlier measurements as passing their original strict limits.
- The source-list/status change already in flight is finished: indexed scalar seeks skip duplicate source pairs, and live/trash counts use separate indexed ranges. Read-only probing of the owned 100k fixture measured four-source retrieval around 0.1 ms versus about 9–14 ms for DISTINCT. No search algorithm change was made. Database regression tests passed 10/10, including equal names/different executables, empty names, unknown executable exclusion, deleted records and the 200-source bound; desktop compilation passed. The first added test used an invalid mutation name and was corrected to the existing `delete` operation before the passing run.
- The packaged focus-fix pipeline passed at 2026-09-10T02:33:06.668Z, including runtime and repeated quick-window focus/restart checks. The subsequent database source-read change above is compiled locally but still needs incorporation into the deliverable package; this is packaging work, not a request for another performance campaign.

- The isolated Web/package refresh for the 50 ms debounce passed all phases at 02:24:32 UTC (`verification-web-refresh-state.json`). Native IME acceptance also passes without clicking or explicitly focusing search: the window's automatic focus receives the OS keys and commits 你好. A desktop-only refresh now incorporates the stale-blur fix and adds five real packaged main-to-quick focus handoffs to the UI verifier; that newest package is still pending acceptance. Controller/native probe/startup probe targeted lint passed before the final test-only additions.

- Real Windows IME acceptance exposed a stale-blur race: the quick HWND was the OS foreground window but a delayed blur handler had hidden it, so search received no keys. The controller now defers blur handling one turn and verifies current renderer/OS focus before hiding. After desktop compilation, `node scripts/verify-clipboard-native.mjs --native-ime` passes: native composition events, Enter committing `nihao` without paste, Space committing **你好** without paste, ordinary Enter positive paste control, and actual blur still hiding the quick window. Paste transport is intercepted only in this fixture; no system clipboard write or input-layout change occurs. Evidence: `.verification/clipboard-v2/native-ime-report.json`. This verifies the currently active Windows input method, not every installed IME. The new desktop focus fix still needs a package refresh after the ongoing Web rebuild completes.

- Expanded renderer search coverage to unique numbers, rare phrases and common English/Chinese terms, and added 30 warm-window reopen samples. The broader indexed set initially exceeded the 200 ms budget (p95 205.9 ms), despite unique-number-only results passing. Reducing the search debounce from 75 to 50 ms gives p95 **176.2 ms** for the broader indexed set, **422.3 ms** for short terms and **57.6 ms** for warm open to visible/focused/enabled search. Warm-open timing includes automation IPC overhead and excludes physical hotkey dispatch. This UI change requires a new isolated Web build/package refresh; the earlier successful package still contains the 75 ms debounce.

- The renderer benchmark rerun after packaging passed: 30 measured samples after four warmups per group, indexed unique-number queries p95 **114.7 ms** (200 ms budget), short present/absent queries p95 **427.9 ms** (500 ms budget). Timing spans the input event, 75 ms debounce, real preload/IPC/worker query, React update and two animation frames, using the independent 100k-row dataset copy. This proves the measured search cases, not warm popup focus, native capture-to-render, large images/file collections, storage contention or all query distributions. Complete raw samples and scope are stored in the successful `.verification/clipboard-renderer-performance/run-*/report.json`; the earlier startup-timeout report remains separate.

- **Latest package refresh passed at 02:03:29 UTC:** desktop compilation, FFI fallback tests, electron-builder, package/runtime verification and actual packaged UI/quit/restart acceptance all exited 0. The package includes the startup handoff fix. Authoritative state: `X:/release-workspaces/clipboard-2D11wt/verification-desktop-refresh-state.json`; current UI evidence: `.verification/clipboard-package/packaged-ui-report.json`. The pending desktop refresh marker is complete. Earlier failed runs remain retained. This closes this bounded package/runtime/ordinary restart check, not the broader native application matrix, actual IME/display, forced termination/update or end-to-end performance requirements.

- `node scripts/verify-startup-handoff.mjs` passed with real Electron navigation: an HTTP response is held pending, the production intro continuation handler requests handoff, and the application page loads only after cancellation settles. Releasing the late intro response cannot displace that page. The watchdog-specific ordering is covered separately by the startup unit test. Evidence is under `.verification/startup-handoff/`; no system clipboard writes occur. The initial probe's premature 200 ms timer was replaced by triggering continuation only after the server observes the pending request, plus a 30-second process watchdog.

- The desktop refresh passed package/runtime checks but exposed a real startup navigation race during UI acceptance: the intro watchdog released the same BrowserWindow before `loadFile(startup.html)` settled, allowing its late navigation events to abort the application page. `createStartupWindow` now stops and awaits that navigation before releasing the handoff; portable smoke uses the same barrier. Seven startup tests and desktop typecheck pass, including a pending-navigation watchdog case. The failed run is retained as `verification-desktop-refresh-startup-failure.log`/`-state.json` in the isolated workspace. A new package rebuild is running; this startup fix is not yet packaged-UI accepted.

- Added `scripts/benchmark-clipboard-renderer.mjs` for a separate 100k-row copy and the actual controller/preload/worker/React search path, including debounce and two animation frames. Its first run produced **no timing samples**: Electron/DevTools launch timed out after 120 seconds during concurrent packaging, with a network-service restart in the launch log. Failure evidence: `.verification/clipboard-renderer-performance/run-3c740b35-19c7-414b-8277-53fc1a92b99f/report.json`. The cause is not established; rerun after packaging settles. This does not satisfy the renderer performance gate.

- The corrected packaged UI verifier waits for the authenticated main page instead of treating the initial startup document as the application page. It passed with the actual first package: quick/shelf/manager hydration, production search and native IPC, normal quit/restart, cross-surface body/note draft recovery, saving an edited copy, and preserving the original. Evidence: `.verification/clipboard-package/packaged-ui-report.json` and four `packaged-*.png` screenshots. The initial pipeline's UI failure remains recorded; this separate successful rerun does not erase it. The latest desktop correction is now being compiled/repackaged by `.verification/clipboard-package/refresh-desktop.mjs`, with state in the isolated workspace's `verification-desktop-refresh-state.json`.

- The first isolated Windows package passed `verify-packaged-web.mjs`: authenticated quick/shelf pages returned 200, all 11 compiled clipboard modules matched the snapshot, unpacked Koffi loaded, and the SQLite worker ran from ASAR outside developer dependency paths. Evidence: `X:/release-workspaces/clipboard-2D11wt/verification-package.log`. This first package predates the desktop FFI fallback/source-exclusion correction; the pending desktop refresh must pass before final package acceptance.
- `node --test lib/clipboard-layout-browser.test.mjs` passed. Twelve layouts (quick 760×540, shelf 360×420, narrow manager 500×600 at raster scales 1/1.25/1.5/2) have no document horizontal overflow. DOM composition Enter/Escape do not paste or hide the surface; Escape clears search and Ctrl+F focuses it. Screenshots and scope are recorded in `.verification/clipboard-layout/report.json`. These are headless Edge raster-scale and composition-routing checks, not physical display/DPI or real Windows IME acceptance.

## Required delivery

The checkboxes below track product implementation under the user's revised practical acceptance standard. Earlier chronological notes retain the scope and limitations of individual checks; their old “pending” statements are superseded by the latest evidence above. Final package incorporation remains tracked separately until the last build finishes.

- [x] Dedicated quick window: 760×540 DIP, current display, configurable Ctrl+Alt+V, focused search, reset on reopen, correct blur/menu/drag handling.
- [x] Management workspace: responsive resizable list/detail, persistent view state, all/text/image/files/favorites/trash, source/time filtering, safe type-aware previews.
- [x] Persistent screen shelf: reorder, remove independently, hide on close, restore content/geometry, paste/copy/drag.
- [x] Event-driven Windows capture: sequence-consistent multi-format snapshots, source, exclusions, pause/lock/resume baseline, feedback suppression, visible degradation.
- [x] Native paste: target token, foreground verification, modifier release, exactly Ctrl+V, safe failure, never submit/Enter.
- [x] Native image/file drag, file-reference existence checks, copy rather than historic cut, managed 24-hour drag files. Native transport verified with an owned external Windows Forms receiver; broader application and packaged acceptance remain separate gates below.
- [x] Search/cursor paging/virtualization: highlighted matched snippet, query cancellation, stable selection/scroll when new clips arrive.
- [x] Multi-select/range/order/merge preview; text separators; compatible file/image batches; no ambiguous mixed paste.
- [x] SQLite worker, WAL, content-addressed files, reference-aware GC, migrations with row-level recovery report and old archive retained.
- [x] Long-term retention, adjustable 1 GiB budget, 90% warning/full pause, 2 MiB text/64 MiB payload limits, seven-day trash.
- [x] Favorites/remarks/edit-as-copy/partial copy, text/image save-as, archive import/export, integration with full application backup.
- [x] Typed and validated trusted IPC; scoped asset access; sanitized rich preview without remote loads; no automatic model/cloud sharing.
- [x] Light/dark and compact/expanded/batch/shelf/error screenshots; current Windows input method and keyboard/layout checks.
- [x] Existing unit/worker/browser/real Windows and packaged desktop acceptance, with 100k-row timing accepted by the user. Exhaustive matrices and strict tail-latency gates are no longer required.
- [x] Final delivery package includes the last source-list/status change.

## Constraints

Windows first. Linux supports capability-based degradation. No OCR, sequential paste, cloud sync, or scripts this release. Preserve unrelated existing workspace edits. Never run `next build` during dev.

## Progress

### Native dependency degradation and unknown sources

- Moved Koffi loading into `WindowsClipboard` construction, inside the runtime's guarded Windows initialization. Missing native bindings no longer throw while importing the application modules. History, manual collection and manual copy remain usable through polling fallback; automatic paste reports the manual-paste fallback.
- Keep the native degradation warning visible after a successful collection instead of clearing it as a transient capture error. When application exclusions are configured, unidentified sources are not recorded. A clipboard with no owner is reported as unknown instead of being attributed to an unrelated foreground application.
- Desktop compilation and two targeted regression cases passed. The tests inject unavailable FFI with a mock Electron clipboard and a real SQLite worker, verify history/copy/manual-paste behavior, persistent degradation feedback, exclusion enforcement and cleanup. The separate source-provenance case verifies that absent ownership is not replaced with foreground metadata. These are fault-injection checks, not a claim that the full packaged application has been tested with a missing binary.
- This desktop-only fix was added after the initial package snapshot. `.verification/clipboard-package/pending-desktop-refresh.json` records the exact source hashes to refresh; the successful Web output can be reused. The desktop must be recompiled and repackaged through electron-builder, including its executable ASAR-integrity update, and verified again before final acceptance.

### Actual cross-process Windows file drag

- `node scripts/verify-clipboard-native.mjs --native-drag` passed on the actual Windows desktop at scale factor 1. Production quick-window rows trigger Electron's native drag IPC; real mouse input carries the OLE FileDrop payload into an independently launched Windows Forms process. One image and a two-entry file/folder collection arrived with `Copy` effect. The complete received PNG SHA-256 matches the stored asset; original file and folder-child contents remain unchanged.
- The verifier checks the receiver PID and both window handles before mouse-down, releases its own mouse button on failure, and restores the pointer only if it has not subsequently moved. It writes no system clipboard data. The first attempt timed out during cold Electron/DevTools launch; a later pre-input handle check caught the console launch flag also hiding the receiver form. Explicitly showing its verified handle resolved the test setup issue. DPI awareness is set for the test thread and restored after verification.
- Durable result: `.verification/clipboard-v2/native-drag-report.json`. Existing worker reconnection, scoped asset, untrusted IPC, drag preparation and quit-draft barrier checks also passed in this run. This proves actual cross-process Windows drag transport; it does not claim Explorer/editor/chat compatibility, other DPI configurations or the fresh installation package were exercised.
- Repeated the actual native drag with Chinese names, spaces and an emoji in the file path. Both the Unicode file and folder paths arrived intact; the complete image hash and original file contents still matched. The durable report records `unicodeAndSpacePaths: true`.

### Packaged clipboard acceptance in progress

- Added the standalone clipboard page's required client-reference manifest to release staging. Electron explicitly unpacks Koffi and its platform native sidecar; the license inventory now includes those shipped dependencies.
- `scripts/verify-packaged-clipboard.mjs` compares the packaged clipboard modules to compiled source and launches the packaged executable against an isolated ASAR copy, exercising Windows FFI and the real SQLite worker without developer dependency fallbacks or system clipboard writes. `scripts/verify-packaged-clipboard-ui.mjs` exercises the actual packaged quick/shelf/manager pages, search, ordinary application quit/restart and unfinished-edit recovery. These scripts are acceptance gates, not passing evidence until run against a fresh package.
- The standalone ASAR worker probe passed under the installed Electron runtime: an archive-contained worker persisted and read back Unicode text. The existing packaging regression suite passed all eight cases. Neither result substitutes for the fresh packaged application's full acceptance.
- The initial isolated dependency copy was interrupted by the host/session restart; its process handles and processes were confirmed missing. Resumed file copying reuses the incomplete dependency tree. Source was resnapshotted from the current worktree: 1,560 regular files, no extra source files; hashes are recorded in the isolated workspace's `verification-snapshot.json`. Production builds run only in that independent release workspace, preserving the development `.next` directory.
- Compared all 1,560 snapshot inputs to the existing `smart-shell-verify-23b77a10` release workspace before considering artifact reuse. It differs in actual clipboard UI, draft persistence, controller/runtime/store and preload code, so its build cannot satisfy current clipboard acceptance. The clipboard package must use its own current snapshot.
- Desktop TypeScript compilation in `X:\release-workspaces\clipboard-2D11wt` passed. The release runner waits on the specifically verified live Robocopy process and checks its actual exit code before starting Web production compilation, standalone staging and desktop compilation. Its state/log are `verification-build-state.json` and `verification-build.log` in that workspace. Build-only environment limits page-generation workers to one and Node heap to 3 GiB. This is still a running build pipeline, not completed packaged acceptance.
- The independent release build subsequently passed all four phases: editor assets, Next production build (including TypeScript and all 36 static pages), standalone staging and desktop compilation. The clipboard client-reference manifest was explicitly staged. Windows directory packaging has started using the verified Electron 43.4.0 distribution; final package/runtime/UI acceptance remains pending.

### Earlier implementation history

- Initial inspection: repository still contains v1 clipboard implementation. Electron 43.4.0 / Node 24 supports node:sqlite and FTS5 trigram (verified in plan phase).
- Scope and acceptance remain as approved in the conversation; unchecked items are outstanding.

### Desktop integration and first native acceptance

- `main.ts` now starts `ClipboardController` / `ClipboardRuntime`, registers the scoped image protocol and opens a dedicated quick window. The shortcut is present in the shared desktop/web registry and Settings. V1 sampling, UI and tests asserting old retention limits have been removed; legacy migration remains in the v2 database.
- Added quick/shelf renderer entry, shared management workspace, 52-DIP virtual rows, type/source/date search, safe rich preview, remarks, edit-copy, ordered multi-selection, merge preview, trash/undo, settings and transfer-station adapter. These are connected to the typed `historyV2` preload bridge.
- Native test exposed a Windows thumbnail failure for extensionless hashes. Fixed using a temporary PNG hard link and asynchronous shell thumbnail generation with bounded caching. Scoped original-image and thumbnail reads now pass in Electron.
- `lib/clipboard-v2-browser.test.mjs` passed: paste/plain semantics, cross-filter selection order, virtual DOM count, blocked remote rich content, remark failure retention, new-copy scroll preservation, narrow layout and layered Escape. Captures include manager light/dark/narrow.
- `scripts/verify-clipboard-native.mjs --native-paste` passed in isolated Electron: foreground activation through a registered test hotkey, real Ctrl+V into the verified test textarea with no Enter, native event listener, repeat-copy count 1 → 2, scoped PNG and thumbnail responses, rejection of an unregistered same-origin renderer. Report: `.verification/clipboard-v2/native-report.json`. Quick and shelf native screenshots exist.
- This native test uses another window of the isolated test process. **Different external processes/applications and packaged-product acceptance are still required.** No unchecked delivery row above should be interpreted as complete from this test alone.

### Remaining acceptance work (reconciled 2026-09-10)

- Archive export/import and application encrypted backup are implemented and covered by archive/worker/application restore tests. Final packaged desktop backup transport acceptance remains required.
- The 100k worker benchmark, broader renderer search cases and controller-to-focused-search warm-open timings pass with the 50 ms debounce. Remaining performance evidence: physical hotkey dispatch, capture-to-visible, separately reported cold startup, large-image encoding, file collections, storage contention, memory/handle/request behavior over sustained use. The source change to 50 ms is currently in an isolated Web/package rebuild.
- Native image and Unicode file/folder collection drops into a separate Windows Forms OLE receiver passed; original files remain present. This is completed transport evidence. The approved browser/editor/chat/Explorer format matrix, privilege differences and actual application-specific drag behavior remain open.
- Narrow navigation, ordered selections/merge output, mixed grouping and durable draft recovery are implemented and tested. The package with the startup handoff correction passed ordinary quit/restart, cross-surface recovery and edit-as-copy preservation. Packaged backup transport, update/forced-termination behavior and the remaining modal focus paths still need evidence; the newer debounce package will rerun the ordinary package checks.
- Worker reconnection and bounded request admission are implemented below. Continue checking degraded-platform thumbnails, source/file events, capture-budget concurrency and remaining resource failures. Validate pause/lock/excluded formats and multi-format file lists in real desktop tests.
- Design and implemented light/dark, compact/expanded/batch/shelf/error screenshots exist. Twelve headless raster-scale layouts and DOM composition routing pass; actual Windows input method, physical display/DPI changes, small work area and complete keyboard paths remain distinct acceptance items. Native dependency packaging and license checks passed with the previous source snapshot and will repeat for the latest rebuild. Final requirement-by-requirement audit remains open.

### Visual design v3 and archive delivery

- User requested design images before further UI development. Generated three imagegen design sheets under `docs/design/clipboard/`, preserved the full prompt set, and implemented their layout and visual hierarchy in the production clipboard components. The app's actual theme variables now feed isolated UI acceptance; the earlier fixture-only green palette was removed.
- Added `ClipboardToolbar`: on-demand source/date filters, more-actions popover, visible recording state, consistent AliIcon controls, compact quick surface and shelf header. Main actions use the existing black/white primary-button tokens. Manager narrow navigation keeps the mounted list and its position while showing details. Shelf uses variable-height content cards and persistent top/up/down menu operations.
- Archive import validates structure, payload limits, hashes and full footer integrity in staging, then commits in one database transaction; snapshots retain asset references during live capture/GC. Application backup uses an internal desktop snapshot handshake and encrypted archive inclusion. Restore builds a new validated DB before journal admission and rolls back both application and clipboard on failure. Overlapping custom restore roots are rejected before journal admission.
- Archive, worker, database and application backup tests: 13 passed in the latest combined run, including new shelf menu moves and edit-copy result IDs. Browser acceptance covers menus, custom merge preview, import conflict/error/cancel flows, selecting the new edited copy and narrow navigation. `.verification/clipboard-v3/` holds actual component screenshots using fictitious fixture data, including light/dark manager, quick, shelf, expanded preview, batch and error states.
- Full requirement audit remains open. Design images and browser fixture screenshots do not stand in for final packaged native, input-method, multi-display or 100k performance acceptance.
- v3 visual closeout: root/desktop typecheck, desktop compilation and targeted lint passed. Isolated Electron UI smoke passed (scoped image/thumbnail and untrusted IPC checks); system clipboard writes were not requested in this visual run. Fixed the smoke harness to await the new-window event rather than racing `application.windows()` before Playwright observes the untrusted test window. Report preserved separately as `.verification/clipboard-v3/native-ui-report.json`.

### Search, browsing continuity and unsaved edits

- Query workers now yield between at most 1024 candidates for one/two-character terms. Query generations and cancellation are scoped to the trusted renderer; late cancellation cannot cancel a newer generation or another window. Unsubscribe and renderer teardown cancel outstanding queries. SQL list projections return bounded 260-character previews, leaving full representations to detail reads.
- Added forward/backward keyset paging for history and shelf, including tied timestamps and inclusive reopen anchors. The renderer retains at most 500 summary rows, preserves the visible record while trimming/prepending or refreshing after mutations, and saves the manager's actual visible-record bookmark. This is a view-memory bound, not a history retention limit. Fresh capture at the unselected head refreshes to the new record.
- Added an unsaved-edit dialog for record/filter changes, preview hiding, relevant mutations and clipboard actions. Save failure keeps the dialog, pending action and inputs. Remarks save to the original; edited text saves as a new record. Quick-window blur/reopen retains dirty edits instead of resetting the preview. Actual component screenshot: `.verification/clipboard-v3/unsaved-edits.png`. Whole-app unmount/exit recovery remains open.
- Initial 100k benchmark failed: short-search p95 913 ms, status 617 ms. Query-plan inspection found a full wide-row scan for source names. A covering source-name index reduces status work; removing redundant import FTS rewrites avoids unnecessary index churn. Bounded 256 MiB memory-mapped reads plus chunked scans reduce Windows read overhead while retaining FULL + WAL durability.
- Latest benchmark: `.verification/clipboard-performance/run-cb4b916a-5804-42ad-9803-630ae8c92e03/benchmark.json`. Windows, i7-9750H, 16 GiB RAM, Node 24.5.0, 100k mostly bilingual text/file-reference records, 30 warm samples per measurement. p95: first page 4.2 ms; indexed search 72.6 ms; short search 391.5 ms; cancellation 3.7 ms; capture persistence plus summary read 34.7 ms; status 21.5 ms. Cold worker opening was 557 ms and process RSS about 298 MiB. These measurements cover worker/database boundaries only, not end-to-end window opening, actual capture-to-render, large images, constrained RAM or packaged Electron.
- Database (9), archive (4), worker (1), browser (1), and encrypted application backup (1) cases passed across the relevant runs. Browser coverage now includes over 1000 rows, bounded cached pages, backward browsing, mutation/reopen anchors, live head capture, all three draft-dialog choices, failed-save retention and dirty quick-window reopen. Root/desktop typecheck and desktop compilation passed; isolated Electron UI smoke again passed without requesting native clipboard writes.

### Native drag preparation and independent-process paste

- Drag preparation now uses per-renderer generations. Late success or failure cannot replace the current selection. Unsubscribe, mutation, renderer loss and window teardown revoke prepared capabilities. UI refresh prepares the current selection again; a missing file at drag start produces a renderer-visible error rather than a silent no-op.
- Added a bounded 256-entry in-memory PNG reuse cache with shared in-flight preparation. Drag files are independent copies, so a destination modifying a file cannot mutate the archive. Copies changed or moved by a destination are recreated. Temporary copy writes use rename; interrupted `.tmp` files and old thumbnail sources are cleaned only when app-named and over 24 hours old. Original file references are never deleted or retimed.
- Prepared-window references protect cache files from cleanup. Actual drag start renews the 24-hour lifetime. Cleanup serializes with new preparation, and shutdown drains admitted preparation. Two file-cache tests cover sharing, archive independence, recreation, actual-use retention, protected references and conservative cleanup. Production Electron IPC tests cover reversed completion order and missing-file errors. **This proves preparation and retention, not actual OS drop delivery.** The API still follows Electron's [native drag-start flow](https://www.electronjs.org/docs/latest/tutorial/native-file-drag-drop/).
- Extended the native verifier with an explicitly requested `--external-paste` case. It launches an owned Windows PowerShell / WinForms RichTextBox in a distinct process, verifies PID and foreground HWND, then uses the production quick window and paste service. Actual external text and bold RTF formatting matched; Enter count stayed zero. Holding a real test-owned Ctrl key returned `modifiers-held`, with release guaranteed in cleanup. Killing the owned target process returned `target-lost` without further input.
- Added partial SendInput cleanup: if only part of Ctrl+V was inserted, issue key-up events for keys this operation could have pressed. Native fault injection of a one-event insertion verified the Ctrl-up recovery and `input-blocked` result. This is an explicit simulation of partial insertion, not a claim that Windows itself produced that failure in the test.
- `node scripts/verify-clipboard-native.mjs --native-paste --external-paste` passed with exit 0. Durable mode-specific report: `.verification/clipboard-v2/external-paste-report.json`; later UI-only runs no longer overwrite this evidence. Native event capture/repeated-copy count, PNG/thumbnail requests and untrusted IPC rejection also passed. Root/desktop typecheck, desktop compilation, targeted lint, browser regression and both cache tests passed.
- Remaining gates retain their full scope: actual browser/editor/chat/Explorer workflows across all representations, real file/image drops, privilege differences, packaged product, input methods and multiple display/DPI settings. One owned native editor process does not satisfy that entire application matrix.

### Content details, grouping and storage feedback

- Extracted `ClipboardBatchBar`. Mixed text/file selections expose text and image/file groups. Entering a group preserves its original order and offers restoration of the complete selection. Ambiguous normal copy/paste buttons are disabled; keyboard actions explain how to choose a group without reading every payload first. Favorites, deletion and shelf management remain available for mixed selections. Browser acceptance verifies both groups, exact output IDs and restored ordering; `quick-mixed-groups-dark.png` records the state.
- Details show Unicode character/line counts, image dimensions/bytes, file/folder counts and type labels. RTF-only content explains that plain text is previewed while the original formatting remains available for paste. Valid web links display their hostname and open only on an explicit button action. The trusted bridge accepts a record ID, rereads that record and rejects unsupported schemes, credentials, invalid text and raw URL arguments. Native IPC acceptance stubs the OS browser launch and verifies the validated destination, so it does not claim actual default-browser integration was exercised.
- Rich preview construction is memoized and iterative. It reads at most 256k source characters and visits at most 10k nodes, with a visible shortened-preview notice. The archive's complete HTML is preserved. Browser tests verify the bound and that merely previewing a link does not open it; existing script and remote-resource checks continue to pass. Compact preview spacing leaves more room for text at 760×540.
- Full-budget state now explicitly says collection is paused while history remains usable, with capacity/recycle-bin actions. Settings show content usage/budget separately from database/index/WAL files and legacy backup files. Store tests assert the additional disk measurement; browser tests keep copy enabled while full and verify the settings values.
- Database (9), shared content helpers (2), browser regression (1), targeted lint and desktop compilation passed. Native UI/IPC smoke passed, including URL validation and previous drag preparation checks. Mode-specific native external-paste evidence from the prior pass remains intact. Full localization, application-exit draft recovery and the broader packaged/native/IME/DPI matrix remain open.

### Clipboard interface localization

- All three clipboard surfaces and the transfer-station adapter now use a shared Chinese/English UI catalog. The standalone window has its own language provider; same-origin windows follow the application's saved language through storage events. A language update that arrives during initial hydration also settles hydration correctly.
- Labels, accessibility names, dates, notices, owned migration errors and native menu/dialog copy use the selected language. Clipboard bodies, filenames, source names, titles and user notes remain unchanged. The trusted locale IPC accepts only `en` and `zh-CN`, with locale tracked per renderer.
- Browser acceptance passes in Chinese and English, including a real second browser context page changing the shared language while an unsaved note stays intact. English manager, quick-window and shelf screenshots are in `.verification/clipboard-v3/`. Two catalog tests verify static UI coverage, interpolation fields and preservation of user-provided values. Root/desktop typecheck and desktop compilation passed. Native translated menu/dialog interactions and the broader packaged/IME/DPI matrix still need dedicated acceptance.


### Durable edit recovery and quit acknowledgement

- Added a local IndexedDB recovery store, separate from clipboard history, with strict transaction completion before showing a saved receipt. Each editor instance has its own draft ID; deletion compares the reviewed revision so another window's later edit is preserved. Draft metadata is paged independently of full bodies. Per editor, at most one body write is active and only the newest pending value is retained; a burst test sends 100 large-text updates and verifies at most two transactions plus the exact final content. Drafts have no automatic age/count expiry. Failed writes retain the latest value in memory and offer a visible retry, including after a React workspace unmount.
- An unobtrusive “Unfinished edits” entry opens a recovery dialog. Text and notes remain available after reload or process restart; users can resume against the current original, download the draft, or explicitly confirm its deletion. Changed original notes produce an overwrite notice. Missing original records do not remove recovery content or prevent download. Successful save/discard clears the matching recovery copies; rapid edit/revert cannot resurrect an older write.
- Desktop shutdown now waits for trusted, per-renderer draft acknowledgements before clipboard/runtime teardown. Preload temporarily suspends input while recovery drains. A failed or timed-out acknowledgement restores window interaction and cancels ordinary quit/update installation before services stop. Callback payloads contain only a random token and success state, never draft bodies. This also covers a manager that was unmounted while its recovery module remains loaded.
- `lib/clipboard-drafts-browser.test.mjs` uses a persistent Edge profile and closes/restarts its browser process. It verifies full Unicode text/note recovery, original-note conflict notice, edit-as-copy preserving the original, rapid revert, injected IndexedDB failure/retry, two simultaneous editors of the same record, stale-delete protection, missing-original download, and confirmation scoped to one draft. Existing clipboard browser regression and both catalog cases pass.
- The isolated production Electron verifier confirms the native close barrier waits, the recovery copy is present in IndexedDB before acknowledgement, and an injected flush failure leaves clipboard IPC/runtime usable and restores input. Report: `.verification/clipboard-v2/native-ui-report.json`; this run did not request system clipboard capture or paste. The full packaged desktop restart/update flow, OS-forced termination, and broader native/IME/DPI gates remain separate acceptance items.


### Worker failure recovery and bounded request admission

- Clipboard storage now reports ready/failed/reconnecting state. An interrupted worker rejects all pending calls and clears its request map; the renderer exposes “Reconnect storage” without replacing history with a misleading zero count. The recording indicator shows storage failure, capture pauses, and reconnect loads persisted settings/status before establishing a fresh native sequence baseline. It does not backfill the clipboard held during the interruption.
- Reconnects share one replacement worker, wait for the old worker to actually terminate, ignore late responses from old generations, and never replay unacknowledged mutations/captures. A corrupted database remains untouched; external repair can be followed by reconnection. Runtime/controller initialization can be retried without duplicating listeners or protocol setup.
- Normal admission is bounded at 256 outstanding requests, with 16 additional control slots for cancellation, snapshot cleanup and close. Orderly close stops new admission, settles exports, closes SQL and waits for worker termination. Known snapshot IDs whose creation/export acknowledgement was interrupted are retained until their export workers settle, then cleaned on reconnection or orderly close; unknown prior-run directories are preserved.
- The fault-injection worker test exits after a real SQL capture commit but before its response. All in-flight operations reject, reconnect retains exactly one copy plus original rich text/note/favorite/shelf data, 20 simultaneous reconnects share one worker, stale-generation messages are ignored, admission remains bounded, threads actually exit, and damaged file bytes remain unchanged. Interrupted snapshot cleanup is covered. Worker recovery (1), worker snapshot/search (1), archive (4), and encrypted application backup (1) cases passed.
- Production Electron acceptance terminates the real storage worker and uses the actual reconnect button. History/image detail remains readable. Controlled snapshot/sequence functions verify that no captures happen while disconnected, sequence 102 held during downtime is not backfilled, and the next sequence 103 is recorded once. This is a runtime fault-injection check, not a new claim of native OS clipboard notification coverage. UI/IPC report: `.verification/clipboard-v2/native-ui-report.json`.
- Browser regression includes the disconnected-history state and reconnect notice. Full installation/update, actual native drop/application matrices, input methods, multiple monitors/DPI, long-running resource stress, previous-process orphan cleanup, and end-to-end performance gates remain outstanding.
