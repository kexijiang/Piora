# 性能与交互检查（2026-09-05，当前工作区）

以下发现记录的是 9 月 5 日的检查基线，位置行号也对应当时版本。随后已按用户授权修改代码；当前进度和验证边界见 [OPTIMIZATION_PROGRESS.md](./OPTIMIZATION_PROGRESS.md)，不能把下面的基线描述当作当前未修复列表。

## 9 月 7 日更新

本轮检查列出的优化及相关回归已完成；最终完整测试 1,393 通过、5 跳过、0 失败，网页/桌面类型检查、ESLint 和性能预算检查通过。以下原始发现保留为修复前基线。

- 已实现：完整删除范围预览与二次核对、会话写入及异步清理排空、30 天回收站、增量 SSE、历史消息与流式尾部隔离、聊天和侧栏虚拟列表、共享 Git 快照、设置选项精确定位、能力设置任务入口。
- 搜索已合并并发索引、传递取消信号、限制缓存保留字节数，并统一归档筛选和结果数量。祖先查找改为复用已计算结果。
- 合成搜索场景：6,000 条同轮助手消息的冷索引由约 2,655ms 降至约 21ms；80 个均命中的文件、limit=1 的场景由读取 80 个文件降至 2 个。这是同机合成函数测试，不是浏览器响应时间。
- 实际渲染计划性能检查：10,000 条聊天消息约 14.19ms；导航窗口范围计算约 0.04ms。深达 12,000 层的侧栏树构建和查找已通过非递归测试。
- 9 月 7 日完整测试集：1,376 项通过、5 项环境跳过、0 项失败。后续修改另跑相关定向回归。
- 后续已完成真实应用中的 1,500 行聊天、流式更新、100 条搜索结果、500 项侧栏键盘/拖动、设置精确定位、390px 布局和变更失败恢复验收；批量部分删除失败也已单独复验。真实组件的 10,000 行浏览器回归 8 项通过；恢复旧逻辑的测试副本重现全部挂载。开发服务仍有间歇性超时，已有验收与后续打开失败的记录分别保留，详见进度文档。

## 优先处理

### P1：批量删除归档的实际范围大于确认范围

- 位置：`components/ArchivedChatsSettings.tsx:175`，`app/api/sessions/[id]/route.ts`。
- “删除全部归档”只按归档会话数量确认，随后调用会递归删除子会话的 DELETE 接口。
- 触发：已归档父会话下面仍有未归档、甚至正在运行的子会话。子会话也会停止并进入回收站，但不计入确认数量。
- 建议：批量预览去重后的完整受影响子树，明确未归档数、运行数；部分失败后刷新已成功的状态，避免用户重复操作。

### P1：长会话搜索索引存在平方级工作量

- 位置：`lib/conversation-search.ts:143`。
- 每条助手消息独立遍历 parent 链寻找最近用户消息。同一轮有大量助手/过程消息时，已访问的祖先被反复遍历。
- 临时合成 JSONL 实测（单次、本机、冷索引）：1,000 条助手消息约 72ms；3,000 条约 653ms；6,000 条约 2,655ms。
- 这是索引函数耗时，不是页面实际交互延迟；但该 CPU 工作位于共享的 Next.js 进程，会竞争聊天/SSE 等请求所需的事件循环时间。
- 建议：缓存最近用户祖先，或按分支关系一次计算锚点；保留分叉、缺失父节点和环路的处理。

### P1：历史定位绕过分页限制，流式渲染仍扫描历史

- 位置：`components/ChatWindow.tsx:527`、`:874`、`:1003`。
- 搜索定位把 visibleCount 增至 messages.length × 2，一次挂载全部历史；向上翻页也只增加挂载量，不回收离屏内容。
- 每次聊天组件更新都会重新分组全部历史，并为最终回答构造新对象，削弱 MessageView 的 memo 效果。
- 工作区虽已有 `VirtualList.tsx`，聊天和侧栏尚未接入，不能视为已解决。
- 建议：把历史展示与流式尾部隔离，稳定历史消息引用；搜索与时间线只定位并挂载目标附近窗口。需覆盖滚动锚点、展开过程、代码块延迟加载和回到底部。

### P2：搜索取消只停了前端，结果上限也不限制服务端扫描

- 位置：`app/api/sessions/search/route.ts:18`、`lib/conversation-search.ts:230`、`:242`。
- 请求的 AbortSignal 没有传给文件读取和匹配循环。输入新关键词或关闭窗口后，旧扫描可能继续消耗资源。
- 已达到 limit 时只跳过结果添加，仍扫描剩余会话；临时测试中 limit=1、80 个均匹配的文件，实际读取了全部 80 个。
- 建议：传播取消信号，合并同文件的并发索引任务；明确按“会话最近活动”还是“消息时间”排序，再设计有界扫描或分页，不能盲目早停破坏排序语义。
- 缓存另以 512 个文件为上限，没有文本字节预算，超大会话下仍有内存风险。

### P2：搜索结果显示数量与实际可访问结果不一致

- 位置：`components/ConversationSearchDialog.tsx:90`、`:194`。
- API 请求最多 100 条，界面固定 slice(0, 24)，数量提示却使用 response.results.length；没有加载剩余消息结果的入口。
- 触发：返回 25–100 条消息匹配时，用户只能访问前 24 条；服务端 truncated 也不一定为 true。
- 建议：统一返回/显示上限，或者提供分页；数量提示按实际可访问的结果计算。
- 同文件的标题匹配只特判 archive === "archived"；传入的是 allSessions，故“仅未归档”筛选仍可能在标题匹配中显示归档会话。标题和正文应共享筛选条件。

### P2：Git 面板存在旧结果覆盖新状态的竞态

- 位置：`components/workspace/ReviewPanel.tsx:109`、`:125`。
- status 和 branches 请求没有请求代次或取消检查。项目切换或连续刷新时，先发后到的请求仍可更新界面。
- diffGenerationRef 保护的是 diff，不保护 status/branches 本身；共享缓存也不能阻止旧订阅者更新 React 状态。
- 建议：以 cwd 和请求代次校验结果；组件卸载、项目切换及新刷新时废弃旧订阅。
- 顶栏刷新另在 busy/refreshKey 变化时先清空状态和仓库标识，容易产生闪动；异步旧请求结束还可能重新安排轮询，需要核对调度代次。

### P2：侧栏与时间线仍有无界挂载

- 位置：`components/sidebar/TaskList.tsx:209`、`:266`，`components/ChatMinimap.tsx:353`、`:393`。
- 分支默认展开、递归渲染所有子任务，展开状态随组件卸载丢失。
- 时间线按钮和展开的预览也按全部节点生成，聊天窗口化本身不能解决这部分开销。
- 建议：持久化分支展开状态，扁平化可见树后窗口化；拖拽、键盘焦点、选中任务定位需保留。时间线轨道按像素密度聚合，预览列表单独窗口化。

### P2：设置搜索入口与精确选项索引脱节

- 位置：`components/SettingsDialog.tsx:427`，`lib/settings-search.ts:14`，`components/ConversationSearchDialog.tsx:164`。
- 设置页搜索只匹配类别标题/说明/组名，没有使用已经维护的详细选项索引。
- 全局搜索虽然使用详细索引，点击后却只传 section，丢失具体 item.id，不能定位到目标设置。
- 建议：两个入口共享同一索引及精确选项锚点；动态加载后定位并短暂突出显示选项，保留键盘焦点。

## 设计建议（仍需实际界面验证）

- 工具、能力包、扩展、技能、插件并列，要求用户先理解实现概念。建议增加“安装能力”“启用与配置”“当前项目生效情况”等任务入口，并保留原有深链接。
- 区分已归档与已删除的目的：归档用于收纳，回收站用于恢复；删除前统一说明子任务影响，恢复后应能定位到恢复的任务。
- 不宜优先进行整体换肤。长会话定位、搜索完整性、错误反馈和异步状态正确性更直接影响日常使用。

## 本轮验证

- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- 删除客户端、子树互斥、回收站、删除路由、运行时收尾、SSE 传输相关测试：30 项通过。
- `npm run lint`：1 个错误（SSE route 的 heartbeat 应使用 const）、3 个警告（FileExplorer 未用类型、VirtualList hook 依赖、传输层未用解构变量）。尚未清理，不能宣称检查全部通过。
- 合成搜索测试只使用临时目录，并在完成后清理，未读取用户会话内容。
- 未运行 `next build`。尝试 `next dev --webpack` 后，instrumentation 导入链报 `lib/file-access.ts` 无法解析 `path`；浏览器控制也超时。本轮没有取得可靠页面截图或真实页面帧率，因此不把视觉观感、首屏耗时或滚动流畅度作为实测结论。
- `perf:check` 主要是辅助函数计时和源码模式检查，不能替代真实长会话、流式更新、搜索跳转和大量侧栏任务的交互性能测试。
